#!/bin/su root
WINDOWS_ENTRY=$(grep -i windows /boot/grub/grub.cfg | cut -d "'" -f 2)
if [ -z "$WINDOWS_ENTRY" ]; then
	echo "Windows boot entry not found." >&2
	exit 1
fi
grub-reboot "$WINDOWS_ENTRY" && sudo reboot
